<script type="text/javascript">

  // only for static images. Not things that your users upload
  window.images = {
    sampleAvatar: "<%= image_path("sample_avatar.jpg") %>"
  }
  <% if logged_in? %>
    window.currentUser = <%= render(
      "api/users/user.json.jbuilder",
      user: current_user
    ).html_safe %>
  <% end %>
</script>

<main id="root"></main>
